name: OpenStack Horizon test

# Controls when the workflow will run
on:
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: Build and test
        run: |
          cd openstack
          sh build.sh
